---
title: "An Test post for Rmarkdown"
subtitle: ""
excerpt: "testing the excerpt field"
date: 2022-02-10
author: "YH jang"
draft: false
images:
series:
tags: 'test','rmarkdown'
categories:
layout: single
output:
  html_document:
    toc: TRUE
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
require(data.table)
require(dplyr)
require(gt)
require(gtsummary)
best_cohort <- fread('C:/Users/user/mimic/Mimic-iv/bestcare/cohort_v1.csv')

```


## 1. baseline table
```{r, include=FALSE}

best_cohort %>% 
  select(sex,age,icu_los, apache,crrt,dex_usage, afib_yn,inhos_mortality) %>% 
  tbl_summary(
  by='dex_usage',
  statistic = list(all_continuous() ~ "{mean} ± {sd}",
                   all_categorical() ~ "{p}"),
  digits = list(all_continuous() ~ 1,
                all_categorical() ~ 1),
  type = list(c(age,icu_los,apache) ~ "continuous",
              c(sex,crrt,dex_usage,afib_yn,inhos_mortality) ~ "categorical"),
  missing = "no") %>% 
  add_p()
```

